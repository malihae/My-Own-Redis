{
  "name": "myredis-studio",
  "version": "1.0.0",
  "private": true,
  "description": "Maliha's custom key-value server with a retro web workspace",
  "type": "module",
  "engines": {
    "node": ">=22.9"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server/index.js",
    "dev": "node --env-file-if-exists=.env server/index.js --dev",
    "build": "vite build",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "motion": "^12.0.0",
    "@phosphor-icons/react": "^2.1.10",
    "@fontsource/space-grotesk": "^5.2.0",
    "@fontsource/ibm-plex-mono": "^5.2.0",
    "redis": "^5.0.0"
  },
  "devDependencies": {
    "vite": "^7.0.0",
    "@vitejs/plugin-react": "^5.0.0"
  }
}
